import React from "react";
import styled from "styled-components";

const ColumnStyled = styled.div`
  padding: calc(5px + 0.3vh) calc(8px + 0.3vh);
`;

const Column = ColumnStyled;

export default Column;
